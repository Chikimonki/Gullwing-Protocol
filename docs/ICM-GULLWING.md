# ICM — Gullwing Protocol — Convergent Control Model

**Status:** Production-honest, 100% open source, CISA/ENISA submitted, NCSC pending.

## Control Loop

```
Target Binary
  → [1 IDENTITY] [2 STRUCTURE] [3 SEMANTICS]
    → [4 ENTROPY] [5 ML k-NN 89.7%]
      → [6 RUNTIME strace] → [7 MEMORY] → [8 MEMORY DIFF]
        → CONVERGENCE (risk + novelty)
          → Ed25519 attestation + CycloneDX SBOM + STIX 2.1
            → Cormorant Bus (Elixir) → Dashboard / Quarantine
```

**Primary axis:** 8-layer convergent intelligence — no single layer trusted. Agreement between mirrors produces truth.

## Evidence

- `forensics_capture.sh` → `forensics_report_YYYYMMDD_HHMMSS/` (hashlist, SBOM via Syft, Ed25519)
- `releases/v1.0-*/proof_bundle` — CRA Annex VII bundle
- `reports/` — training corpora, `system.model`

## Guardrails

- Keep `WCC` (Other) + `colibrì` (Apache 2.0) + `Headscale` (BSD-3) attributions in `README → Acknowledgments`
- Keep `MIT` for new code, reproducible builds for Kestrel

## Relation to other ICMs

- **PartyVault ICM v9.0:** `Perl → Zig → LuaJIT → Julia` for *financial* DORA
- **Vivisect ICM:** `Lua → eBPF/Zig → dumps` for *kernel* observation
- **Quantum ICM:** `Zig → LuaJIT → Julia → Groq` for *qubit* twin
- **Gullwing ICM:** `Zig → LuaJIT → QEMU/Ollama → Elixir` for *binary* intelligence

---
*Pan Enterprises, Liverpool — Chikimonki — pan283@gmail.com*
